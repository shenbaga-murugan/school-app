import { CommentBoxDirective } from './comment-box.directive';

describe('CommentBoxDirective', () => {
  it('should create an instance', () => {
    const directive = new CommentBoxDirective();
    expect(directive).toBeTruthy();
  });
});
